import { StyleSheet, Text } from "react-native";

const Footer = () => {
    return (
        <>
            <Text style={styles.container}>Footer</Text>
        </>
    )
}

export default Footer;

const styles = StyleSheet.create({
    container: {
      marginTop: 50,
    },
});